# Only direct dependencies are listed; pip resolves compatible versions of
# transitive packages (werkzeug, jinja2, cryptography, bcrypt, pynacl, ...)
# automatically. The previous file pinned transitive dependencies to versions
# that do not exist on PyPI, which broke `pip install -r requirements.txt`.
flask>=3.0
paramiko>=3.4
pyyaml>=6.0
